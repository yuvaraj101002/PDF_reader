/**
 * Dictionary service (MVP milestone 5) — offline lookups against the bundled
 * SQLite dictionary (built by scripts/build-dictionary/):
 * tap word → lemmatize → query → { definitions, ipa, examples, synonyms }.
 */
export {};
