/**
 * Reader engine (MVP milestone 3) — reflowable rendering of BookContent:
 * paragraph-virtualized (FlashList), selection popover (Highlight / Note /
 * Speak / Define), inline highlight spans, TTS karaoke tracking.
 * Scroll mode first; pagination after the measurement utility exists.
 */
export {};
